const nextConfig = {
    /* config options here */
    reactCompiler: true,
    pageExtensions: ['js', 'jsx', 'ts', 'tsx'],
    images: {
        domains: ['images.unsplash.com'],
    },
};

export default nextConfig;
